# 贡献教程

## 文件要求

- 每个软件一个 Markdown 文件，放在 `docs/tools/`。
- 文件名使用小写英文与连字符，例如 `my-tool.md`。
- 一级标题写软件名称，后续步骤使用二级、三级标题。
- 截图放在 `docs/assets/images/`，使用有意义的英文文件名。
- 示例令牌必须使用占位符，禁止提交真实密钥。

## 动态数据核验

模型 ID、模型单价、订阅套餐、充值规则和分组倍率等内容会随平台调整。修改这些内容时必须：

1. 登录 CodeFlow 控制台，按页面中标注的来源路径核对数据。
2. 同时更新页面 frontmatter 中的 `dataVerifiedAt`，日期格式统一为 `YYYY-MM-DD`。
3. 同时更新正文「数据来源与核验」提示中的核验日期和来源；来源应写明控制台页面名称或公开 URL。
4. 只有实际查看来源并完成逐项核对后才能推进核验日期，不得直接使用文件编辑日期或提交日期。
5. 若实时数据与文档不一致，应先更新数据、示例配置和相关截图，再更新核验日期。

## 提交前检查

```bash
npm install
npm run docs:build
```

确认构建没有报错、图片可显示、站内链接可访问后再提交。
